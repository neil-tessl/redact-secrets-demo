# tessl-labs/redact-secrets

Detect and redact secrets in text — API keys, tokens, credentials.

When sharing logs, snippets, or pastes that might contain sensitive credentials, scan first and replace them with placeholders that preserve shape without leaking the original.

## Install

```
tessl install tessl-labs/redact-secrets
```
